# generated by octocov init
coverage:
  paths: 
  if: true
testExecutionTime:
  if: true
comment:
  if: is_pull_request
summary:
  if: true
diff:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}/base
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}/base
